<Window x:Class="SlideCreater.CreaterEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SlideCreater"
        mc:Ignorable="d"
        Title="Slide Creater" Height="800" Width="1000">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Menu>
            <MenuItem Header="File">
                <MenuItem Header="New"></MenuItem>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="5"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"></GridSplitter>

            <Grid Grid.Column="0">
                <TextBox x:Name="TbInput" TextWrapping="WrapWithOverflow" SpellCheck.IsEnabled="True" AcceptsReturn="True" AcceptsTab="True"></TextBox>
            </Grid>
            <Grid Grid.Column="2">
                <StackPanel>
                    <Border Background="HotPink">
                        <local:SlideContentPresenter x:Name="FocusSlide"></local:SlideContentPresenter>
                    </Border>
                    <ScrollViewer Height="300">
                        <StackPanel Name="slidelist">
                            
                        </StackPanel> 
                    </ScrollViewer>
                    <Button Click="Test_Button">Render</Button>
                    <Button Click="Show">Show</Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
