<Window x:Class="CCUPresetDesigner.CCUPresetDesignerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:CCUPresetDesigner"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1200">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="CommonStyles.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <ScrollViewer>
                <ListBox Name="lbPulpit"></ListBox>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Column="1">
            <ScrollViewer>
                <ListBox Name="lbCenter"></ListBox>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Column="2">
            <ScrollViewer>
                <ListBox Name="lbLectern"></ListBox>
            </ScrollViewer>
        </Grid>

    </Grid>
</Window>
